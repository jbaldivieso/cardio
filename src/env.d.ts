/**
 * The version from package.json, substituted at build time by the `define` in
 * `vite.config.ts` (and mirrored in `vitest.config.ts`). The settings screen
 * shows it (§7.8).
 */
declare const __APP_VERSION__: string
